- Tuned session presets to a realistic ~65 wpm sustained pace, so the word goal and the timer finish together instead of one going slack.
- Added Archive — a read-only window for browsing and searching your past writing sessions (⌘L).
